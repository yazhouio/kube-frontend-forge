{
  "id": "crd-page-state",
  "schema": {
    "templateInputs": {
      "PAGE_ID": {
        "type": "string",
        "description": "Page id"
      },
      "CRD_CONFIG": {
        "type": "object",
        "description": "CRD store config"
      },
      "SCOPE": {
        "type": "string",
        "description": "Scope name"
      },
      "HOOK_NAME": {
        "type": "string",
        "description": "Hook name"
      }
    },
    "outputs": {
      "params": { "type": "object" },
      "refetch": { "type": "object" },
      "toolbarLeft": { "type": "object" },
      "pageContext": { "type": "object" },
      "data": { "type": "object" },
      "loading": { "type": "boolean" },
      "update": { "type": "object" },
      "del": { "type": "object" },
      "create": { "type": "object" }
    }
  },
  "generateCode": {
    "imports": [
      "import { useMemo } from \"react\"",
      "import { buildSearchObject, getCrdStore, usePageStore, useProjectSelect, useRuntimeContext } from \"@frontend-forge/forge-components\""
    ],
    "stats": [
      {
        "id": "storeDecl",
        "scope": "moduleDecl",
        "code": "const useStore = getCrdStore(%%CRD_CONFIG%%);",
        "output": ["useStore"],
        "depends": []
      },
      {
        "id": "fallbackCommandDecl",
        "scope": "moduleDecl",
        "code": "const __crdTableFallbackCommand = \"kubectl label crd \" + (%%CRD_CONFIG%%).plural + \".\" + (%%CRD_CONFIG%%).group + \" kubesphere.io/resource-served=true\";",
        "output": ["__crdTableFallbackCommand"],
        "depends": ["storeDecl"]
      },
      {
        "id": "hookDecl",
        "scope": "moduleDecl",
        "code": "const %%HOOK_NAME%% = (columns, storeOptions = undefined) => {\n  const pageId = %%PAGE_ID%%;\n  const page = usePageStore({ pageId, columns });\n  const runtime = useRuntimeContext();\n  const params = runtime?.route?.params || {};\n  const pageContext = runtime?.capabilities || {};\n  const storeQuery = useMemo(() => buildSearchObject(page, true), [page]);\n  const scope = %%SCOPE%%;\n  const { render: renderProjectSelect, params: { namespace: selectNamespace } } = useProjectSelect(\n    { cluster: params.cluster },\n    { enabled: scope === \"namespace\" },\n  );\n  const namespace = scope === \"namespace\" ? selectNamespace : undefined;\n  const toolbarLeft = () => scope === \"namespace\" ? renderProjectSelect() : null;\n  const store = useStore({ params: { ...params, namespace }, query: storeQuery }, storeOptions ?? {});\n  return {\n    params,\n    toolbarLeft,\n    pageContext,\n    data: store.data,\n    loading: Boolean(store.isLoading || store.isValidating),\n    refetch: store.mutate,\n    update: store.update,\n    del: store.batchDelete,\n    create: store.create,\n  };\n};",
        "output": ["HOOK_NAME"],
        "depends": ["storeDecl"]
      }
    ],
    "meta": {
      "inputPaths": {
        "storeDecl": ["CRD_CONFIG"],
        "fallbackCommandDecl": ["CRD_CONFIG"],
        "hookDecl": ["HOOK_NAME", "PAGE_ID", "SCOPE"]
      }
    },
    "defaults": {
      "CRD_CONFIG": "{}",
      "PAGE_ID": { "type": "dataSourceIdJson" },
      "SCOPE": "\"cluster\""
    }
  }
}
